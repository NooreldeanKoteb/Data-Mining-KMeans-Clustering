To run the code for iris dataset simply run k_means_clustering.py in Part 1 directory


To run the code for the Image dataset run k-means-images.py in the Part 2 directory.
k-means-images.py uses k_means_clustering.py as a library.